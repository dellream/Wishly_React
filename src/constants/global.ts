export enum MEDIA {
    DESKTOP = 'desktop',
    TABLET = 'tablet',
    MOBILE = 'mobile'
}

export enum BREAKPOINTS {
    TABLET = 768,
    DESKTOP = 1200
}

export enum PATH {
    MAIN = '/',
    REGISTRATION = '/registration',
    PROFILE = '/profile',
    NOT_FOUND = '*',
    LOGIN = '/login',
    FRIENDS = '/friends',
    CREATE_WISHLIST = '/create-wishlist'
}
